FUNCTION Read ( ) ;

FUNCTION FizzBuzz ( )
  Read ( n ) ;
  IF n MOD 3 = 0 THEN
    IF n MOD 5 # 0 THEN 
      PRINT ( "Fizz" )
    ELSE
      PRINT ( "FizzBuzz" )
    ENDIF
  ELSE
    IF n MOD 5 = 0 THEN 
      PRINT ( "Buzz" )
    ENDIF
  ENDIF
END.

